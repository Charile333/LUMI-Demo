# GitLab CI/CD - 市场监控
# 推荐每15分钟运行一次（*/15 * * * *）以保持在免费额度内

market-monitor:
  image: node:18
  only:
    - schedules  # 只在计划任务触发时运行
  script:
    - echo "🔍 开始市场监控..."
    - node scripts/monitor-market.js
  variables:
    SUPABASE_URL: $SUPABASE_URL
    SUPABASE_KEY: $SUPABASE_KEY

